# Changelog

## Unreleased

- Initial local-first OSS core.
- Added `init`, `doctor`, `plan`, `run --once`, `loop`, `pr`, and `status` commands.
- Added command routes, model routes, gates, branch-backed commits, run artifacts, and block-level execution history.
